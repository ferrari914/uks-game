# Englknight — 작업 보드

## 세션 명단 (메시징 이름)
> 세션이 재시작되면 ListAgents 이름이 바뀐다. 각 역할은 이름이 바뀔 때마다 자기 행을 갱신한다 (직원은 담당 관리자가 갱신).
>
> **더 간단한 방법:** 이 표는 금방 낡는다. `mcp__ccd_session_mgmt__list_sessions`를 부르면
> sessionId와 함께 역할이 드러난 제목(overseer / game-manager / 테트리스 게임 관리 /
> Ox 서바이벌 게임 / 메인 홈페이지 담당)이 그대로 나오고,
> `mcp__ccd_session_mgmt__send_message`는 이름이 아니라 **sessionId로 보낸다.**
> sessionId는 재시작해도 안 바뀌므로 표 갱신 없이 항상 정확하다.
> (ListAgents 메시징 이름은 세션 안에서 바꿀 수 없다 — 도구가 없다.)

> **sessionId 열이 진짜 주소다.** 메시징 이름은 재시작마다 바뀐다 —
> 실제로 게임 관리자는 claude-1b → claude-32 → claude-db로 두 번 바뀌었다.
> 이름 열은 참고용이고, 확실하게 보내려면 sessionId를 쓴다.

| 역할 | sessionId (불변 · 진짜 주소) | 세션 제목 |
|---|---|---|
| 총괄 관리자 | `local_c4e559f1-5783-435a-ac8b-fa2e52117426` | Englknight 총괄관리자 |
| 게임 관리자 | `local_2a3b71bd-e26b-44bb-8104-297c3df7ffc4` | game-manager |
| 메인 홈페이지 담당 | `local_749a2582-ee5a-4325-b9fb-022eb6315a1a` | 메인 홈페이지 담당 |
| 테트리스 담당 | `local_9ab90326-2ecf-441c-907f-31926b540730` | 테트리스 게임 관리 |
| OX 서바이벌 담당 | `local_65021d38-9c4c-48c9-a2e3-26653308441d` | Ox 서바이벌 게임 |
| 마피아 담당 | `local_05227cc1-2e07-4c29-ab46-f3abf99ece1f` | 마피아 게임 담당 |
| 라이어 게임 담당 | `local_c0a198ed-b78e-4852-a6cc-f56dd867287d` | 라이어 게임 담당 |
| 캐치마인드 담당 | `local_32e6ebd3-1c6b-462b-ad2b-5aa0eb6b6d9d` | 캐치마인드 게임 개발 |
| 무한의 계단 담당 | `local_57933acb-d0b1-44d2-b6cc-0ff12eec3af4` | 무한의 계단 담당 |
| 영어 끝말잇기 담당 | `local_89bf5575-763a-41c1-af4a-06928730a2c8` | 영어 끝말잇기 |
| 1대1 매치 담당 | `local_e45cb62f-840d-4473-8353-2beb35deca80` | 1대1 매치 담당 |

> **위 값은 2026-08-28 `list_sessions` 실제 조회 결과다.** 이름(ListAgents `claude-xx`)은
> 재시작마다 바뀌므로 적지 않는다. 담당이 스스로 보고한 sessionId가 조회 값과 다른 사례가
> 있었으니(무한의 계단·1대1), **명단 갱신은 반드시 `list_sessions` 조회로 확인하고 적는다.**

운영 규칙:
- 이 파일이 전체 작업 현황의 단일 소스다. 작업 착수/완료 시 반드시 갱신한다.
- 갱신 권한: 총괄 관리자, 게임 관리자, 메인 홈페이지 담당. (직원은 담당 관리자에게 보고 → 관리자가 갱신)
- 항목 형식: `- [담당] 작업 내용 — 상태 메모`

## 진행 중
- [무한의 계단 담당] 무한의 계단 — **폴더·조사자료 준비 완료**(`games/stairs/REFERENCE_무한의계단.txt`). 사용자 확정: 첫 화면에서 싱글/멀티 선택, 싱글은 최고 층수 랭킹, **멀티는 2인 이상 동시 등반 → 한 명 남을 때까지 무한 경쟁**. 충돌 판정이 없어 공용 MQTT에 적합(각자 층수·생존 여부만 공유). **착수 지시 하달 완료.** SPEC.md 선제출 → 게임 관리자 검수 → 구현
- [영어 끝말잇기 담당] 영어 끝말잇기 — **폴더·브리프 준비 완료**(`games/wordbattle/BRIEF.md`). Englknight(English+knight) 컨셉에 정면으로 맞는 게임이 현재 없어 그 자리를 메운다. **종목 = 영어 끝말잇기로 사용자님이 직접 확정**(2026-08-28). **착수 지시 하달 완료.** SPEC.md 선제출 → 게임 관리자 검수 → 구현
- [1대1 매치 담당] 1:1 대전 — **종목 = 오목으로 사용자님이 직접 확정**(2026-08-28). — **폴더·브리프 준비 완료**(`games/duel/BRIEF.md`). 현재 멀티가 전부 다수 인원이라 **둘이서 붙을 게임이 없다.** 오목/커넥트4 등 종목은 담당이 확정. **착수 지시 하달 완료.** SPEC.md 선제출 → 게임 관리자 검수 → 구현
- [전 담당] **코드 최적화 라운드 (2026-08-28, 사용자 지시)** — 동작 변경 없이 순수 최적화. 이번 라운드는 직접 커밋 금지: 각 담당 보고(3줄+지문+요약) → 총괄 검수 → 총괄 커밋·배포. 발령 완료: 테트리스/OX/홈페이지/라이어/캐치마인드. 마피아는 v2 검수·배포 후 착수. 검수·커밋 현황: (대기)
- [캐치마인드 담당] 캐치마인드 v1 제작 — 스펙 확정(`games/catchmind/SPEC.md`), 담당 직원 2명 구현 중(제시어/엔진 분업). **실시간 멀티 유지**(공용 MQTT·방장 권위, OX 서바이벌과 동일 구조 — 그림 브로드캐스트가 게임 본질이라 싱글 축소 불가). 개인전 2~8명·방장 수동 시작, 팀전 2:2~4:4, 라운드 3/5/7/10. 완료 시 담당 1차 검수 → 게임 관리자 검수 → 사용자 보고
- [게임 관리자] 라이어 게임 v1 — **제작 완료, 게임 관리자 검수 통과(결함 0건).** 패스 앤 플레이 3~10인, 기본/바보/스파이. 미커밋. 잔여: ①역전승 시 지목 보너스 지급 여부 사용자 판단 대기 ②실기기·`file://` 사용자 검증 ③허브 `ready` 전환 요청
- [게임 관리자] 마피아 게임 v1 — **제작 완료, 게임 관리자 검수 통과(결함 0건).** 미커밋. 잔여: ①AI 대사 템플릿 반복 개선 여부 사용자 판단 대기 ②`data/games.js` 등록(메인 홈페이지 담당에 항목 전달 완료) ③커밋 대기
- [메인 홈페이지 담당] 라이어·마피아 허브 등록 — **완료·배포됨.** 커밋 `9ef3ab9`(라이어 등록 + 마피아 v2 문구) · `05b9e86`(마피아 wip 임시 강등) · `3a15efe`(마피아 ready 활성화), 전부 푸시 완료. 배포 허브 게임 5개 전부 ready.
  - 마피아 검수 미해결이던 "시작 버튼 무반응"은 **버그가 아니라 인원 조건**으로 확인됨 (사용자 실측: 싱글 정상, 멀티는 4~8인 필요한데 혼자여서 미시작). 다만 **인원 미달을 화면이 알려주는지는 미확인** — 무반응이면 고장으로 읽히므로 `4명부터 시작 (현재 1명)` 류 안내 필요 여부를 마피아 담당·게임 관리자에 전달함
- ~~[메인 홈페이지 담당] 마피아 v1 허브 등록~~ — v1 커밋 요청은 총괄의 지문 대조로 "편집 중" 상태를 잡아내 보류됐다(검증본 `183eb496…` 61,344B 23:33 → 요청 직후 `530b054b…` 65,626B 23:54). v2 완결 후 위 항목으로 통합됨. **교훈: 커밋 요청 시 파일 지문을 함께 넘기면 편집 중 커밋을 막을 수 있다.**
- [메인 홈페이지 담당] 메인 홈페이지 개선 — 총괄 검수 통과, 커밋 완료(b5d8329). 잔여: `file://` 더블클릭 확인(사용자). ~~사이트 정체성 문구~~ → 태그라인으로 해소됨

## 대기 (완성 연락 오면 착수)
- [메인 홈페이지 담당] 신규 게임 허브 등록 — **마피아 등록 완료.** 캐치마인드·라이어는 담당이 `ready` 통보하면 즉시 등록 (카드 정보 수령 완료, 아래 확정분). **`data/games.js`는 메인 홈페이지 담당만 수정한다** — 게임 담당 직접 수정 금지 (동시 수정 충돌 방지)

  | | 캐치마인드 | 라이어 게임 |
  |---|---|---|
  | id / path | `catchmind` / `games/catchmind/index.html` | `liar` / `games/liar/index.html` |
  | 설명 | 제시어를 그림으로! 친구들과 실시간으로 그리고 맞히는 그림 퀴즈. | 한 기기를 돌려가며 즐기는 3~10인 파티 추리 게임. 라이어를 찾아내라. |
  | 태그 | `그림` `퀴즈` `멀티` (신규: 그림) | `파티` `추리` `심리` (신규: 파티) |
  | 이모지 / 색상 | 미정 | 🎭 / 보라 계열 (게임 내 액센트 `#a97bff`에 맞춤) |
  | 구조 | 온라인 멀티 (OX와 동일: mqtt.js CDN + 공용 브로커). `file://`에선 멀티 불가, 안내 표시 | 패스 앤 플레이 (한 기기, 오프라인). 네트워크 없음 |
  | 상태 | 구현 착수 단계 | 구현 중 |

  > **마피아 v2(멀티) 완료 시 카드 문구 교체 예정** — v2에서 실제 유저와 하는 게임이 되므로
  > 현재 `desc`("AI 7명과 겨루는 8인 클래식전")가 부정확해진다. 담당 확정 문구:
  > ```js
  > desc: "친구들과 방을 만들어 함께 하는 실시간 마피아. 혼자서도 즐길 수 있습니다.",
  > tags: ["추리", "멀티", "싱글", "심리"],
  > ```
  > v2 검수 통과 통보를 받은 뒤에 교체한다. **v1이 배포 대상인 동안은 현재 문구가 맞다.**
  > (v2 멀티는 `crypto.subtle` 사용 → https/localhost에서만 동작. 싱글은 `file://`로도 동작)
  >
  > **`status: "ready"`는 게임 관리자 검수 통과 후에만 올린다** (2026-08-27 게임 관리자와 합의).
  > 검수 전에 등록이 필요하면 `wip`으로 먼저 올린다 — 미검증 게임이 허브에 "플레이 가능"으로
  > 노출되는 것을 막기 위해서다.
  >
  > **주의:** 라이어에 `싱글` 태그를 달지 말 것 — 패스 앤 플레이는 한자리에 모인 3~10명이 기기 1대를 돌려보는 오프라인 파티 게임이라, `싱글`을 달면 필터에서 테트리스와 묶여 성격이 정반대로 전달된다. `멀티`(온라인)도 아니다.
  >
  > **허브 가이드라인 7장의 "외부 라이브러리·CDN 금지"는 허브 파일 전용이다.** 게임 폴더에는 적용되지 않는다 — OX 서바이벌이 이미 mqtt.js를 CDN에서 로드해 배포·승인된 선례. (2026-08-27 메인 홈페이지 담당이 게임 담당들에게 잘못 확대 전달했다가 캐치마인드 담당 지적으로 정정)

- [메인 홈페이지 담당] 방명록 — **완료·배포됨** (`d75a550`). `guestbook/index.html` 신설, 허브 헤더에 진입점 추가.
  공용 MQTT retain 메시지 30슬롯 링 버퍼. 브로커 폴백 없음(방문자마다 다른 목록을 보게 되므로 단일 브로커).
  이름 12자·본문 200자·15초 쿨다운·출력 전체 이스케이프, 자기 글만 삭제 가능(localStorage 기록).
  - **구조적 한계 3건 (사용자 보고 완료, 서버 없이는 해결 불가)**: ①토픽을 아는 사람은 누구나 읽기·쓰기·삭제 가능
    ②공용 브로커가 retain을 비우면 글이 사라짐(영구 보관 아님) ③스팸·욕설 차단 수단 없음(제한은 전부 클라이언트 쪽)
  - 실제 보관·차단이 필요하면 외부 백엔드(Firebase 등)로 이전해야 하며, 사용자 계정·키가 필요한 별도 결정
  - 총괄에 파일 추가(`guestbook/`)·CDN 사용(mqtt.js) 판정 요청 전달함 — 허브 계열에서 CDN은 첫 사례

- [메인 홈페이지 담당] 오목·무한의 계단 허브 등록 — **커밋 `61bcbba`, 푸시 대기.**
  오목 `ready`(⚫ / 나뭇결 갈색 카드 / `보드`·`1:1`·`멀티`), 무한의 계단 `wip`(🪜 / `아케이드`·`멀티`).
  무한의 계단은 `games/stairs/`가 미커밋이라 `ready`로 올리면 404 — 커밋·배포 확인 후 `status`만 바꾼다.

  > **태그 기준 (2026-08-28 게임 관리자와 합의):**
  > 새 태그는 **"이 태그 없이는 못 찾는 게임이 있는가"** 로 판단한다.
  > 태그는 분류표가 아니라 **찾는 수단**이다. 분류상 하위 개념이어도 그것으로만
  > 도달할 수 있는 게임이 있으면 남기고(`1:1` — 나머지 멀티가 전부 3~10인),
  > 상위 태그와 갈리는 게임이 없으면 뺀다(`반응속도` → `아케이드`에 흡수).
  > 신규 게임 담당은 태그 제안 시 이 근거를 한 줄 붙여 전달한다.
  > 칩이 늘면 모바일에서 첫 카드까지 스크롤이 길어진다 — 실측 200px → 148px(한 줄 감소).

## 백로그
- [총괄→사용자] OX 서바이벌 멀티플레이 실기기 검증 — 배포 URL에서 PC+폰 2대 테스트 필요, 사용자 직접 테스트 대기
- ~~[게임 관리자] 옷입히기 게임 제작~~ — **취소 확정 (2026-08-27)**: 프로필 화면으로 대체. `games/dressup/PLAN.md`는 a9 참고자료로 유지, 폴더 삭제 여부는 추후 사용자 확인
- [메인 홈페이지 담당] 썸네일 이미지 교체 — 이모지+그라데이션 → 실제 이미지, 프로필 화면 완성 후 진행

## 완료
- [OX 담당] OX v1.3.0 — 채팅(대기실·게임 중)·점수전 모드·타이머 조기판정 수정·버전 정보. 총괄 검수 통과, 보안 3건 사용자 승인(그대로 유지), 커밋 d6e9fdb 배포 (2026-08-27)
- [테트리스 담당] 테트리스 v1.4.0 — 일시정지 종료 버튼·랭킹 중복 등록 수정. 총괄 보안 검토 통과, 사용자 승인, f5f1996 배포 (2026-08-27)
- [메인 홈페이지 담당] 태그라인 "Play. Grow. Knight." + 탭 제목·meta RPG 컨셉 통일 — 총괄 검수 통과, 배포 (2026-08-27). 배포 repo명은 현행 유지로 종결
- [메인 홈페이지 담당] 사이트명 Englknight 변경 + 허브 파비콘 ⚔️ — 총괄 검수 통과, 커밋 ab25416·8f6f2e6 배포 (2026-08-27)
- [메인 홈페이지 담당] 캐릭터 프로필 화면 MVP + 옷입히기 게임 목록 제외 — 총괄 정식 검수 통과, 커밋 ccade8d·b5d8329 배포. 잔여: `file://` 저장 동작 확인(사용자), 레벨·스탯은 placeholder (2026-08-27)
- [OX 담당] 붕괴 연출 고도화 + "대명" 표기 제거 — 1차·최종 검수 통과, 커밋 976fb33 배포 (2026-08-27)
- [총괄] 허브 메인 페이지 제작 및 GitHub Pages 배포 — https://ferrari914.github.io/uks-game/ (2026-08-27)
- [총괄] 테트리스, OX 서바이벌 허브 등록 (2026-08-27)
