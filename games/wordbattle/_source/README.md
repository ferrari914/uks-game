# 굽기 원본 (빌드 입력 — 배포물 아님)

- `enable1.txt` — ENABLE 워드리스트 172,819개. 퍼블릭 도메인 (Alan Beale).
  출처: https://norvig.com/ngrams/enable1.txt (2026-08-28 내려받음, 사용자 승인)
- 이 폴더는 **`dict.js`를 굽는 입력**이다. 게임은 구워진 `dict.js`만 쓴다.
- 런타임에 이 파일을 읽지 않는다 → 정적 배포·`file://` 제약과 무관.
- **커밋 제외 대상.** 배포물에 원본까지 넣을 이유가 없다(중복 1.7MB).
