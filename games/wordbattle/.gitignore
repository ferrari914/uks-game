# 굽기 원본만 제외한다 — 1.7MB짜리 워드리스트를 히스토리에 넣을 이유가 없다.
# 출처와 sha256은 _source/README.md와 SPEC.md §2.2에 적어뒀으니 언제든 다시 받을 수 있다.
_source/enable1.txt

# 굽는 스크립트와 손으로 쓴 원본(TSV)은 커밋한다 —
# 나중에 사전을 갱신할 사람이 같은 결과를 재현할 수 있어야 한다.
