# Englknight — 프로젝트 가이드

## 프로젝트 개요
- **프로젝트명**: Englknight (구 UK's Game, 2026-08-27 개명. English + knight — "영어를 쓰는 기사" 컨셉)
- **목적**: 나만의 캐릭터를 키우는 RPG 사이트. 미니게임(테트리스, OX 서바이벌 등)은 캐릭터 성장 수단이고, 프로필 화면에서 아바타를 꾸민다. 추후 PvP 콘텐츠 예정
- **핵심 기능**:
  - 메인 페이지에 게임들을 카드 그리드 형태로 표시
  - 카드 클릭 시 해당 게임 페이지로 이동
  - 게임 추가/삭제/수정이 쉬운 데이터 기반 구조 (게임 목록은 하나의 데이터 파일에서 관리)
- **디자인 방향**: 다크 테마, 게임 아케이드 느낌, 모바일 대응

## 폴더 구조 (현재)
```
욱스게임/
├── CLAUDE.md            # 이 파일
├── index.html           # 허브 메인 페이지
├── assets/
│   └── style.css        # 허브 공용 스타일 (다크/아케이드)
├── data/
│   └── games.js         # 게임 목록 데이터 (단일 소스)
└── games/
    ├── tetris/          # 테트리스 (플레이 가능)
    └── dressup/         # 옷입히기 게임 (준비중 placeholder)
```

### 데이터 파일에 대한 메모
게임 목록은 `data/games.js` 하나에서 관리한다 (`window.GAMES` 배열).
JSON 대신 JS를 쓰는 이유: `index.html`을 더블클릭해 `file://`로 열 때
`fetch('games.json')`은 브라우저 보안정책(CORS)에 막혀 목록이 비어 보인다.
`<script src>`로 불러오는 JS는 로컬에서도 그대로 동작한다.
배포 후 JSON으로 바꾸고 싶으면 그때 전환한다.

## 운영 규칙: 조직 체계

```
사용자 (정동욱) ─ 최종 결정권자. 모든 관리자·담당에게 직접 지시한다
 ├─ 총괄 관리자 (Fable 5) ─ 비전 제시·검수 전담. 커밋/푸시/배포, CLAUDE.md·TASKS.md 관리
 ├─ 게임 관리자 ─ games/ 소속 담당 직원 관리·1차 검수 (OX 서바이벌 담당 등)
 ├─ 테트리스 담당 ─ 사용자 직결 (게임 관리자 경유 없음)
 └─ 메인 홈페이지 담당 ─ 허브 파일(index.html, assets/, data/games.js) + 프로필 화면
```

- **지시는 사용자가 각 담당에게 직접 내린다 (2026-08-27 개편).** 총괄 관리자는 사용자 요청이 있을 때만 하향 통보·지시를 전달하며, 지시가 필요하다고 판단되면 임의로 보내지 않고 먼저 사용자에게 묻는다.
- **총괄 관리자의 목적은 비전 제시와 검수다.** 결과물 최종 검수·승인이 핵심 역할이고, 직접 제작하지 않는다.
- **관할 경계**: 게임 담당은 자기 게임 폴더 안만 수정한다. 허브 파일(`index.html`, `assets/`, `data/games.js`)은 메인 홈페이지 담당 관할이다. 서로의 영역은 요청 없이 건드리지 않는다.
- **커밋·푸시 권한은 총괄 관리자만 갖는다.** 다른 역할과 직원은 파일만 만들고, 검수 통과분만 총괄이 커밋한다.
- **검수 체계 (2026-08-27 저녁 개정)**: `games/` 소속 작업(테트리스·OX 등)은 게임 관리자가 검수해 **사용자에게 직접 보고**한다 (총괄 경유 없음). 허브·프로필(메인 홈페이지 담당) 작업은 총괄이 검수한다. 커밋 전 최종 확인은 커밋 권한자인 총괄이 한다.
- **보고 양식**: "변경 파일 / 테스트한 것 / 남은 문제" 세 줄을 기본으로 한다.
- **QA는 절차로 한다**: 게임 하나가 완성되면, 그 게임을 만들지 않은 검수 직원을 1회성으로 스폰해 플레이 테스트한다.
- **작업 현황은 `TASKS.md`에서 관리한다.** 착수/완료 시 갱신.
- **세션 식별은 `TASKS.md` 상단 "세션 명단"으로 한다.** 메시징 이름(claude-xx)은 재시작 시 바뀌므로, 다른 세션에 메시지를 보내기 전 명단을 확인하고, 자기 이름이 바뀌면 자기 행을 갱신한다.
- **모델 구성**: 총괄 관리자는 Fable 5, 직원(서브 에이전트)은 항상 `model: "opus"` (Opus 5)로 생성한다.
- 작업이 작고 단순하면(파일 1~2개 수정 수준) 관리자가 직접 처리해도 된다. 결과물 검수는 생략하지 않는다.

## 작업 원칙
1. **게임 추가는 항상 같은 절차로**: `games/` 아래 폴더 생성 → `data/games.js`에 항목 추가 → 허브에 자동 반영되는지 확인
2. **기존 게임 코드는 함부로 건드리지 않는다.** 수정 요청이 있을 때만 해당 게임 폴더를 변경한다.
3. **커밋 단위는 작게**: 기능 하나, 게임 하나 단위로 커밋한다.
4. **큰 구조 변경(폴더 구조, 배포 방식 등)은 실행 전에 나에게 먼저 계획을 보고하고 승인을 받는다.**
5. 사용자(나)가 실행을 요청한 작업이라도, 되돌리기 어려운 작업(파일 삭제, 강제 푸시 등)은 실행 전에 한 번 더 확인을 받는다.

## 배포
- GitHub Pages 배포 완료: https://ferrari914.github.io/uks-game/ (repo: ferrari914/uks-game, main 브랜치 루트)
- 총괄 관리자가 커밋·푸시하면 자동 재배포된다.

## 현재 상태
> 진행 중인 작업 현황의 단일 소스는 `TASKS.md`다. 아래는 프로젝트 마일스톤 요약이며, 상세·실시간 현황은 TASKS.md를 본다.
- [x] 허브 메인 페이지(index.html) 제작 — 카드 그리드, 검색, 태그 필터, 모바일 대응
- [x] 게임 데이터 구조 확정 — `data/games.js` (id/title/desc/path/emoji/color/tags/status/added)
- [x] 테트리스 등록 — `games/tetris/` 로 이동 후 허브 연결 확인
- [x] OX 서바이벌 등록 — `games/ox-survival/` (실시간 멀티플레이, MQTT/WebSocket 기반 — 자세한 내용은 해당 폴더 README.md)
- [ ] 캐릭터 프로필(아바타 꾸미기) 화면 제작 — RPG 컨셉: 옷입히기는 게임이 아니라 프로필 화면이다 (담당: 메인 홈페이지 담당)
- [ ] 썸네일 이미지 교체 (지금은 이모지 + 그라데이션 임시)
- [x] 배포 설정 — GitHub Pages (https://ferrari914.github.io/uks-game/)

## 게임 추가 체크리스트
1. `games/<게임id>/index.html` 생성
2. `data/games.js` 배열에 항목 1개 추가
3. 로컬 서버에서 허브 카드 → 게임 진입 확인
   ```bash
   python3 -m http.server 8765
   ```
